name: solar-system CI
on:
  workflow_dispatch:
  push:
    branches:
      - main
      - 'feature/*'
      
env:
  MONGO_URI: 'mongodb+srv://supercluster.d83jj.mongodb.net/superData'
  MONGO_USERNAME: ${{ vars.MONGO_USERNAME }}
  MONGO_PASSWORD: ${{ secrets.MONGO_PASSWORD }}

jobs:
  unit-testing:
    strategy:
      matrix:
        node-version: [18]
        os: [ubuntu-latest,macos-latest]
        exclude:
          - node-version: 18
            os: macos-latest
            
    runs-on: ${{ matrix.os }}
    
    steps:
      - name: Install Node For Application
        uses: actions/setup-node@v7
        with:
          node-version: ${{ matrix.node-version }}
          
      - name: Checkout The Appliction Codebase
        uses: actions/checkout@v7
        
      - name: Install Dependencies
        run: npm install
        
      - name: Run Tests
        run: npm test

      - name: Upload The Test File
        uses: actions/upload-artifact@v7
        with:
          name: mocha-test-result-node-${{ matrix.node-version}}-os-${{ matrix.os}}
          path: test-results.xml

  code-coverage:
    strategy:
      matrix:
        node-version: [18]
        os: [ubuntu-latest]
            
    runs-on: ${{ matrix.os }}
    
    steps:
      - name: Install Node For Application
        uses: actions/setup-node@v7
        with:
          node-version: ${{ matrix.node-version }}
          
      - name: Checkout The Appliction Codebase
        uses: actions/checkout@v7
        
      - name: Install Dependencies
        run: npm install
        
      - name: Run Code Coverage
        continue-on-error: true
        run: npm run coverage

      - name: Upload The Test File
        uses: actions/upload-artifact@v7
        with:
          name: code-coverage-node-${{ matrix.node-version}}-os-${{ matrix.os}}
          path: coverage
